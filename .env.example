# Dockerized PostgreSQL with pgvector extension
# Docker maps localhost:5433 -> container:5432
# Used for native vector search and cosine similarity
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/semantic_cache_db